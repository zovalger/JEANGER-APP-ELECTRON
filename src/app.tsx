import { createRoot } from "react-dom/client";
import "./index.css";

const root = createRoot(document.body);

root.render(
	<>
		<h2 className="px-2">hola</h2>
		
		<h1 className="text-gray-700 font-bold text-3xl">gola</h1>
	</>
);
